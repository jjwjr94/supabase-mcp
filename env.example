# Supabase MCP HTTP Server Environment Variables

# Required: Supabase Personal Access Token
# Get this from your Supabase dashboard > Account > Access Tokens
# This is required for accessing the Supabase Management API
SUPABASE_ACCESS_TOKEN=your_supabase_access_token_here

# Required: Supabase Project Reference
# Get this from your project settings in Supabase dashboard
SUPABASE_PROJECT_REF=your_project_ref_here

# Optional: Supabase Database Password
# Get this from your project settings in Supabase dashboard
SUPABASE_DB_PASSWORD=your_db_password_here

# Optional: Supabase Region
# Get this from your project settings in Supabase dashboard
SUPABASE_REGION=us-east-1

# Optional: Supabase Service Role Key
# Get this from your project settings in Supabase dashboard > API
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: Enable read-only mode for database operations
# Set to 'true' to prevent INSERT operations (SELECT still allowed)
# Set to 'false' to allow both SELECT and INSERT operations
SUPABASE_READ_ONLY=false

# Optional: Comma-separated list of features to enable
# Available features: account, branching, database, debugging, development, docs, functions, storage
SUPABASE_FEATURES=docs,account,database,debugging,development,functions,branching

# Security Configuration
# Optional: Comma-separated list of allowed project references
# If empty, all projects are allowed
SUPABASE_ALLOWED_PROJECTS=

# Optional: Comma-separated list of allowed database schemas
# Default: public
SUPABASE_ALLOWED_SCHEMAS=public

# Optional: Comma-separated list of allowed table names
# If empty, all tables are allowed (subject to schema restrictions)
SUPABASE_ALLOWED_TABLES=

# Optional: Comma-separated list of blocked operations
# Example: apply_migration,deploy_edge_function,create_project
SUPABASE_BLOCKED_OPERATIONS=

# Server configuration
PORT=3000
NODE_ENV=development
